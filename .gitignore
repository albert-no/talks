.DS_Store
**/.DS_Store
scripts/.cache/

# Local Claude Code overrides — user-specific; team settings live in .claude/settings.json
.claude/settings.local.json

# Standalone bundles are generated on demand by scripts/bundle.py for
# distribution/hand-off — never committed.
*.standalone.html
